/**
 * Curate programs users can do within range
 */

import React from "react";
import { DateRange } from "../shared";

export interface MilitaryShiftProgramsProps {
  range: DateRange;
}

const MilitaryShiftPrograms = (props: MilitaryShiftProgramsProps) => {
    const data = []
    return (
        <div className="military-shift-programs">
        </div>
    )
}

export default MilitaryShiftPrograms;

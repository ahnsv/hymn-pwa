import React, { ReactNode } from "react";

interface HymnStepIndictorProps {
  steps: number;
  currentStep: number;
  isActive?: boolean;
}
const stepIndicator = (props: HymnStepIndictorProps) => {
  
};
